using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SystemScene3 : MonoBehaviour
{
    public bool[] Gameplay;
    // Start is called before the first frame update
    void Start()
    {
        Gameplay = new bool[6];
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
